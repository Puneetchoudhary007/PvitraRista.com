import { SignupComponent } from "./sign-up/sign-up.component";

export class Model {
    constructor( private signupObj: SignupComponent,)
    {
        this.signupObj.firstnameFormControl.value;
            
    }
}